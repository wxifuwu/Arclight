import { Modal } from "../../types";

const modal: Modal = {
    id: "areallyscarytemplatemodalnamesoyoudontaccidentallyuseit",
    execute: (interaction, client) => {
        console.log("hi");
    }
}

export default modal;