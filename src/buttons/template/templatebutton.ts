import { Button } from "../../types";

const button: Button = {
    id: "areallyscarytemplatebuttonnamesoyoudontaccidentallyuseit",
    execute: (interaction, client) => {
        console.log("hi");
    }
}

export default button;